import {ArrowDown,ArrowRight,Download,FileJson,ShieldCheck} from 'lucide-react';
import {Link} from 'react-router-dom';
import {SectionHeading} from '../components/ui/SectionHeading';
import {transferWorkflowStages} from '../data/workflow';
export function WorkflowPage(){return <div className="page-section page-top"><div className="page-title"><span>Phase 2 lifecycle workflow</span><h1>From formula to quality</h1><p>Four specialist applications now exchange versioned, user-controlled JSON files while retaining independent storage and domain ownership.</p></div><div className="transfer-flow">{transferWorkflowStages.map((stage,index)=><div className="transfer-flow-item" key={stage.title}><article><header><span>{stage.number}</span><div><h2>{stage.title}</h2><Link to={`/modules#${stage.moduleId}`}>{stage.module}</Link></div><strong>{stage.status}</strong></header><dl><div><dt>Input</dt><dd>{stage.input}</dd></div><div><dt>Processing</dt><dd>{stage.processing}</dd></div><div><dt>Output</dt><dd>{stage.output}</dd></div><div><dt>Transfer file</dt><dd><code>{stage.transferType}</code></dd></div></dl>{stage.exampleUrl&&<a className="button secondary" href={stage.exampleUrl} download><Download/> Download example JSON</a>}</article>{index<transferWorkflowStages.length-1&&<span className="transfer-arrow"><ArrowRight className="desktop-arrow"/><ArrowDown className="mobile-arrow"/></span>}</div>)}</div><section className="data-movement"><SectionHeading eyebrow="How data moves" title="Explicit files, not a hidden database">Phase 2 deliberately uses local JSON export and import. The user chooses what leaves a module, reviews what enters the next one, and confirms every created record.</SectionHeading><div><article><FileJson/><h3>Versioned contracts</h3><p>Every handoff uses envelope version 1.0.0, typed direction metadata, workspace and record identifiers, plus a transfer-specific payload.</p></article><article><ShieldCheck/><h3>Minimal disclosure</h3><p>Supplier cost is opt-in, panelist-level data never leaves Sensory, and QA receives only limits explicitly confirmed by the user.</p></article><article><ArrowRight/><h3>Additive mapping</h3><p>Existing application IDs and LocalStorage remain valid. Cross-module IDs are optional fields added only when a transfer is used.</p></article></div></section><section className="phase-boundary"><strong>Phase 2 boundary</strong><p>No login, cloud database, background synchronisation, multi-tenant access or silent transmission is present. Transfer history stores metadata only, not full payloads.</p></section></div>}

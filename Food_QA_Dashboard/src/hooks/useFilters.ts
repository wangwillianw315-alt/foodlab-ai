import { useMemo, useState } from 'react'; import type { Filters,FoodQualityRecord } from '../types/quality';
const initial:Filters={product:'',batch:'',status:'',dateFrom:'',dateTo:'',search:''};
export function useFilters(records:FoodQualityRecord[]){const[filters,setFilters]=useState(initial);const filtered=useMemo(()=>records.filter(r=>(!filters.product||r.product_name===filters.product)&&(!filters.batch||r.batch_number===filters.batch)&&(!filters.status||r.status===filters.status)&&(!filters.dateFrom||r.test_date>=filters.dateFrom)&&(!filters.dateTo||r.test_date<=filters.dateTo)&&(!filters.search||[r.sample_id,r.product_name,r.batch_number].some(v=>v.toLowerCase().includes(filters.search.toLowerCase())))),[records,filters]);return{filters,setFilters,filtered,reset:()=>setFilters(initial)}}

import { FilterPanel } from '../components/data/FilterPanel';import { QualityTable } from '../components/data/QualityTable';import { useFilters } from '../hooks/useFilters';import { useQualityData } from '../hooks/useQualityData';
export function DataExplorerPage(){const{records}=useQualityData();const{filters,setFilters,filtered,reset}=useFilters(records);return <div className="space-y-5"><div><p className="eyebrow">Traceability & review</p><h2 className="page-title">Data Explorer</h2><p className="page-subtitle">Inspect, sort and export every sample assessment.</p></div><FilterPanel records={records} filters={filters} setFilters={setFilters} reset={reset}/><QualityTable records={filtered}/></div>}

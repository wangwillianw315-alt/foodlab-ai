import { Component, type ErrorInfo, type ReactNode } from 'react';
export class ErrorBoundary extends Component<{children:ReactNode},{failed:boolean}> { state={failed:false}; static getDerivedStateFromError(){return{failed:true}} componentDidCatch(error:Error,info:ErrorInfo){console.error('Dashboard render error',error,info)} render(){if(this.state.failed)return <div className="mx-auto my-16 max-w-lg rounded-xl border border-rose-200 bg-white p-8 text-center shadow-card"><h1 className="text-xl font-bold text-slate-900">The dashboard could not be displayed</h1><p className="mt-2 text-sm text-slate-600">Your original CSV file has not been changed. Reload the page to restore the application.</p><button className="btn-primary mt-5" onClick={()=>window.location.reload()}>Reload dashboard</button></div>;return this.props.children}}

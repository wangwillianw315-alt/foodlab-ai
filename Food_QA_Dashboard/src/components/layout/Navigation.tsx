import type { Page } from '../../App'; const pages:Page[]=['Dashboard','Data Explorer','Standards','Lifecycle Transfer','About'];export function Navigation({page,onChange}:{page:Page;onChange:(p:Page)=>void}){return <nav aria-label="Primary navigation" className="border-y border-slate-200 bg-white"><div className="mx-auto flex max-w-[1500px] gap-1 overflow-x-auto px-4 sm:px-6 lg:px-8">{pages.map(p=><button key={p} aria-current={page===p?'page':undefined} onClick={()=>onChange(p)} className={`whitespace-nowrap border-b-2 px-4 py-3 text-sm font-semibold transition ${page===p?'border-navy-600 text-navy-700':'border-transparent text-slate-500 hover:text-slate-800'}`}>{p}</button>)}</div></nav>}

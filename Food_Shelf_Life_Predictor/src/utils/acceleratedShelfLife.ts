import{linearRegression,calculateRSquared}from'./statistics';const R=8.314;
export const celsiusToKelvin=(c:number)=>c+273.15;
export function calculateQ10(t1:number,t2:number,r1:number,r2:number){if(![t1,t2,r1,r2].every(Number.isFinite)||t1===t2||r1<=0||r2<=0)return null;const q=(r2/r1)**(10/(t2-t1));return Number.isFinite(q)&&q>0?q:null}
export const estimateRateAtTemperature=(referenceRate:number,referenceTemperature:number,targetTemperature:number,q10:number)=>{if(![referenceRate,referenceTemperature,targetTemperature,q10].every(Number.isFinite)||referenceRate<0||q10<=0)return null;const rate=referenceRate*q10**((targetTemperature-referenceTemperature)/10);return Number.isFinite(rate)?rate:null};
export function fitArrheniusModel(points:{temperatureC:number;rate:number}[]){const valid=points.filter(p=>Number.isFinite(p.rate)&&p.rate>0&&Number.isFinite(p.temperatureC)&&celsiusToKelvin(p.temperatureC)>0);if(valid.length<3)return{valid:false,slope:null,intercept:null,rSquared:null,activationEnergy:null,warnings:['At least 3 valid temperature conditions are required.']};const xy=valid.map(p=>({x:1/celsiusToKelvin(p.temperatureC),y:Math.log(p.rate)})),r=linearRegression(xy);if(!Number.isFinite(r.slope)||!Number.isFinite(r.intercept))return{valid:false,slope:null,intercept:null,rSquared:null,activationEnergy:null,warnings:['Arrhenius regression requires distinct valid temperatures.']};const r2=calculateRSquared(xy,r.slope,r.intercept),activationEnergy=-r.slope*R;if(!Number.isFinite(r2)||!Number.isFinite(activationEnergy))return{valid:false,slope:null,intercept:null,rSquared:null,activationEnergy:null,warnings:['Arrhenius model produced a non-finite result.']};return{valid:true,slope:r.slope,intercept:r.intercept,rSquared:r2,activationEnergy,warnings:r2<.6?['Weak Arrhenius fit. Real foods may not follow a single mechanism.']:[]}}
export const estimateActivationEnergy=(slope:number)=>-slope*R;

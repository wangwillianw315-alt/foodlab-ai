import{describe,it,expect}from'vitest';import{calculateMean,calculateMedian,calculateStandardDeviation,detectIqrOutliers,linearRegression,calculateRSquared}from'../utils/statistics';describe('statistics',()=>{it('summarises values',()=>{expect(calculateMean([1,2,3])).toBe(2);expect(calculateMedian([1,3,2,4])).toBe(2.5);expect(calculateStandardDeviation([1,2,3])).toBe(1)});it('flags IQR only',()=>expect(detectIqrOutliers([1,2,2,2,2,3,100])).toEqual([100]));it('fits regression',()=>{const p=[{x:0,y:1},{x:1,y:3},{x:2,y:5}],r=linearRegression(p);expect(r.slope).toBe(2);expect(calculateRSquared(p,r.slope,r.intercept)).toBe(1)})});

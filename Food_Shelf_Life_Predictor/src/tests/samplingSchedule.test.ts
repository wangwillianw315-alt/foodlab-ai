import{describe,it,expect}from'vitest';import{generateSamplingSchedule,identifyOverdueSamples,calculateCompletionRate}from'../utils/samplingSchedule';describe('sampling',()=>{it('generates planned dates',()=>{const s=generateSamplingSchedule('c','2026-01-01',[0,3,7]);expect(s[2].planned_date).toBe('2026-01-08')});it('identifies overdue safely',()=>{const s=generateSamplingSchedule('c','2020-01-01',[0]);expect(identifyOverdueSamples(s,new Date('2020-01-02')).length).toBe(1)});it('calculates completion',()=>{const s=generateSamplingSchedule('c','2026-01-01',[0,1]);s[0].sample_status='COMPLETED';expect(calculateCompletionRate(s)).toBe(50)})});

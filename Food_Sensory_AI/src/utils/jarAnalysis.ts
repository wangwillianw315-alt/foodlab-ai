import {calculateMean,calculatePercentage,clean} from './statistics';
export const calculateJarDistribution=(v:(number|null)[],points:3|5=3)=>{const a=clean(v),mid=points===3?2:3,low=a.filter(x=>x<mid).length,jar=a.filter(x=>x===mid).length,high=a.filter(x=>x>mid).length;return{tooLow:calculatePercentage(low,a.length),jar:calculatePercentage(jar,a.length),tooHigh:calculatePercentage(high,a.length),count:a.length}};
export const calculateMeanDrop=(jar:(number|null)[],nonJar:(number|null)[])=>{const a=calculateMean(jar),b=calculateMean(nonJar);return a===null||b===null?null:a-b};
export const calculateWeightedPenalty=(drop:number|null,affected:number)=>drop===null?null:drop*affected/100;
export const calculatePenaltyAnalysis=(scores:(number|null)[],liking:(number|null)[],points:3|5=3)=>{const mid=points===3?2:3,pairs=scores.map((s,i)=>({s,l:liking[i]})).filter((x):x is {s:number,l:number}=>x.s!==null&&x.l!==null),jar=pairs.filter(x=>x.s===mid).map(x=>x.l);return(['tooLow','tooHigh'] as const).map(group=>{const g=pairs.filter(x=>group==='tooLow'?x.s<mid:x.s>mid).map(x=>x.l),drop=calculateMeanDrop(jar,g),affected=calculatePercentage(g.length,pairs.length);return{group,jarMean:calculateMean(jar),groupMean:calculateMean(g),meanDrop:drop,affectedPercentage:affected,weightedPenalty:calculateWeightedPenalty(drop,affected),groupSize:g.length,warning:g.length<5?'Insufficient group size for reliable penalty interpretation.':null}})};
